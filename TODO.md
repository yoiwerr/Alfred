# 阿福 — 开发 TODO

> 基于 `docs/阿福.md` 的产品规划，拆分为可执行任务。
> 当前阶段：UI 先行，功能逐步接入。

---

## 阶段一：核心能力实现

### 1. 想清楚：任务澄清 → 任务契约

- [ ] **Planner 升级**：提取维度后输出结构化「任务契约」
  - 字段：最终目标 / 工作范围 / 技术或业务约束 / 验收标准 / 风险边界 / 允许执行的权限 / 期望交付结果
  - 从当前宽泛的 `dimensions` dict 升级为 `TaskContract` Pydantic 模型
- [ ] **Clarify 追问补全**：追问策略对齐全契约字段，缺什么问什么
- [ ] **契约确认 UI**：前端渲染任务契约为卡片，用户可确认/修改
- [ ] **契约版本管理**：确认后的契约入库，后续对话自动注入

### 2. 选对人：能力建议与协作路由

- [ ] **能力卡片数据结构** (`capability_card`)
  - 字段：名称 / 类型(model|skill|tool|agent) / 一句话能力 / 适用场景 / 明显限制 / 推荐表达 / 相关能力 / 信息来源 / 更新时间
- [ ] **能力卡片 RAG 入库**：每张卡一个完整 chunk，标记 `metadata.type = "capability_card"`
- [ ] **Planner 增加推荐判定**：判断是否需要推荐外部能力，生成 `CapabilityRecommendation`
- [ ] **推荐渲染 UI**：聊天气泡内渲染能力推荐卡片（建议对象 + 适用原因 + 下一步帮助）
- [ ] **知识库内容填充**：编写初始能力卡片（ChatGPT/Claude/Perplexity 等模型、Skill、工具能力）
- [ ] **定向 RAG 检索**：对话时优先检索能力卡片，排除用户心智模型数据

### 3. 看住过程：工程规范与安全辅助

- [ ] **动态规范引擎**：根据项目规模动态调整规范等级
  - 小任务（<3步骤）：仅提醒目标确认
  - 中任务（3-10步骤）：目标+范围+Git 提交节奏
  - 大任务（>10步骤）：全量规范（目标/范围/Git/测试/安全/验收）
- [ ] **Checker 节点**：在 LangGraph 图中插入规范检查节点（可选开关）
- [ ] **高风险操作确认**：写文件、执行代码、外部 API 调用前弹确认
- [ ] **产物一致性检查**：Reflector 增加「结果是否满足最初约束」维度

### 4. 记住结果：项目交接卡

- [x] **交接卡按钮**：前端 UI 按钮（当前版本）
- [ ] **交接卡 API**：`POST /api/sessions/{id}/handover`
  - LLM 自动生成结构化交接卡：当前目标 / 已完成事项 / 已确认决策 / 未解决问题 / 下一步行动 / 关键文件 / 新发现的用户偏好 / 下次需验证的信息
- [ ] **上下文快照自动生成**：每个会话结束时自动生成（或手动触发）
- [ ] **快照对比**：新会话开始时对比上次快照，提示变化点

---

## 阶段二：记忆体系重构

### 对齐三层记忆（当前实现 vs 阿福定义）

| 文档定义 | 当前实现 | 差距 |
|---------|---------|------|
| 工作记忆 | L1 滑动窗口 | 缺少结构化字段（目标/阶段/待决/方案），任务结束自动清理未实现 |
| 心智模型 | L2+L3（摘要+事实） | 需增加可信度/更新时间/来源；缺少「不希望被频繁追问」等偏好字段 |
| 能力知识库 | knowledge_base/ | 需拆分：能力卡片 vs 最佳实践 vs 功能说明；增加 metadata 标签 |

- [ ] **工作记忆**：增加结构化字段，任务结束自动清理/压缩
- [ ] **心智模型**：每个事实增加 `source` / `confidence` / `updated_at` / `needs_verify`
- [ ] **知识衰减**：心智模型中的旧信息标记过期，下次对话时可验证
- [ ] **能力知识库拆分**：capability_card → 完整 chunk 不拆分；best_practice → 语义分块；faq → 按需

---

## 阶段三：多 Agent 协作

### 多立场对比

- [ ] **三立场 Agent Panel**
  - 实用派：优先速度和最小实现
  - 稳健派：优先安全和维护成本
  - 创新派：优先差异化与创意
- [ ] **阿福整合**：并行运行三个 Agent，阿福汇总输出结构化对比
  - 共识是什么 / 分歧在哪里 / 每个方案的代价 / 用户需要决定什么 / 阿福倾向于哪个方案及原因
- [ ] **对比渲染 UI**：对比卡片组件，高亮分歧点

### 协作路由

- [ ] **交接材料生成**：当推荐使用外部能力时，自动整理交接说明（任务背景 + 已确认信息 + 期望输出）
- [ ] **外部 Agent 接入**：预留 Webhook/API 接口，让外部 Agent 回报结果

---

## 阶段四：工程与体验

### 安全

- [ ] **只读 Git 分析**：（当前删掉，后期补）读取仓库结构、提交历史、关键决策
- [ ] **文件只读分析**：用户选择本地文件，阿福只读分析
- [ ] **执行 Agent 沙箱**：隔离环境执行，高风险操作需额外授权

### 体验

- [ ] **推荐不频繁确认**：推荐阶段确认大方向，执行阶段才核对具体条件（Skill 安装/权限等）
- [ ] **推进中可打断**：用户随时说「换方向」，阿福重新 Planner
- [ ] **会话恢复提示**：新会话加载上次快照，一句话总结上次进度

---
*最后更新: 2026-07-22*
